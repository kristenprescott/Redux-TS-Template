/* IDK how to use this tbh
 *
 */

// import logger from 'redux-logger';

const middleware = ["123"];
// if (process.env.NODE_ENV !== 'production') {
//   // middleware.push(logger);
// }

export { middleware };
